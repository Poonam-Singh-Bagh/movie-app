import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  container: {
    background: "white",
    width: "100%",
    height: "380px",
    marginTop: "200px",
  },
  spacing: {
    textAlign: "center",
    paddingTop: "60px",
  },
}));

export default useStyles;
